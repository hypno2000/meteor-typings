{
  "private": true,
  "scripts": {
    "build": "cd meteor/1.3 && typings bundle --ambient -o out",
    "test": "tsc -p meteor/1.3/test",
    "build+test": "npm run build && npm run test",
    "lint": "tslint \"**/*.ts\" -e \"**/out/**\" -e \"**/test/**\" -e \"node_modules/**\""
  },
  "devDependencies": {
    "tsc": "^1.20150623.0",
    "tslint": "^3.8.1",
    "tslint-config-typings": "^0.2.3",
    "typings": "^0.8.1"
  }
}
